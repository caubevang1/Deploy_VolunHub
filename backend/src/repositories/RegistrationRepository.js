import BaseRepository from "./BaseRepository.js";
import Registration from "../models/registration.js";

const RegistrationRepository = new BaseRepository(Registration);

export default RegistrationRepository;
