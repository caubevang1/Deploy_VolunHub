import BaseRepository from "./BaseRepository.js";
import EventAction from "../models/eventAction.js";

const EventActionRepository = new BaseRepository(EventAction);

export default EventActionRepository;
